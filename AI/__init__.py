"""Core AI package for Battle-Hardened AI.

This file makes the AI directory a proper Python package so that
packaging tools (like PyInstaller) and runtime imports (e.g.
`from AI import pcs_ai`) work consistently across environments.
"""
